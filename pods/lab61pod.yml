apiVersion: v1
kind: Pod
metadata:
  name: demo-pod
spec:
  containers: 
    - image: busybox
      name: demo-container
      command: ["echo"]
      args: [ "This is a container for a demo command" ]
  restartPolicy: Never